#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../cf-blue-green/bin/cf-blue-green" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../cf-blue-green/bin/cf-blue-green" "$@"
  ret=$?
fi
exit $ret
